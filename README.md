# XNNTest
An attempt to implement a neural network framework myself.

It's probably very inefficient. (Though somehow still pretty fast?)

This branch uses `BigDecimal` instead of `Double`, which is ... a lot worse than `main` branch. Perhaps I should try loading the data statically? 